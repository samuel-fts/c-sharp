using System;

namespace Principal{
    class Aula04{
        static int num=1;//e uma variavel global, com o nome de static para poder ser lida e atribuida a memoria assim que o programa ser executado

        static void Main(){//um metodo que noa retorna funcao 
        //static serve para dizer ao compilador que essa classe sera executada assim que o programa tiver executando
            Console.WriteLine("entrou aqui");
        }
    }
}